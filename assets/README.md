# Иконки приложения

Для генерации иконок iOS поместите сюда:

- **icon.png** — 1024×1024 px, PNG, без прозрачности (альфа-канала)

Затем выполните:
```bash
npx @capacitor/assets generate --ios
```

Это обновит `ios/App/App/Assets.xcassets/AppIcon.appiconset/`.
